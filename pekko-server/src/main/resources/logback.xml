<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- This is a development logging configuration that logs to standard out, for an example of a production
        logging config, see the Apache Pekko docs: https://pekko.apache.org/docs/pekko/current/typed/logging.html#logback -->
    <appender name="STDOUT" target="System.out" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <withJansi>true</withJansi>
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan([%thread]) %boldMagenta([%logger{36}]) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Cluster Events Appender - 结构化JSON日志 -->
    <appender name="CLUSTER_EVENTS" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>
    
    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>8192</queueSize>
        <neverBlock>true</neverBlock>
        <appender-ref ref="STDOUT" />
    </appender>
    
    <!-- 集群事件专用logger - 输出结构化JSON -->
    <logger name="ClusterEvents" level="INFO" additivity="false">
        <appender-ref ref="CLUSTER_EVENTS"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="ASYNC"/>
    </root>
</configuration>
