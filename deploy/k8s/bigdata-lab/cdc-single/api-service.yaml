apiVersion: v1
kind: Service
metadata:
  name: pekko-cdc-single-api
  namespace: bigdata-lab
  labels:
    app: pekko-cdc-single
spec:
  type: ClusterIP
  selector:
    app: pekko-cdc-single
  ports:
    - name: http
      port: 8080
      targetPort: http
